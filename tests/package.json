{
  "name": "syslm-tests",
  "version": "1.0.0",
  "description": "Conformance tests for syslm OpenRouter-compatible API",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:syslm": "TEST_TARGET=syslm vitest run",
    "test:openrouter": "TEST_TARGET=openrouter vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "dotenv": "^17.2.3",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "openai": "^4.77.0",
    "zod": "^3.24.0"
  }
}
